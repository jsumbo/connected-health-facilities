#!/bin/sh
# Refuse commits that still contain Cursor co-author metadata.
if grep -qiE '^[Cc]o-[Aa]uthored-[Bb]y:.*[Cc]ursor|cursoragent@cursor\.com' "$1"; then
  echo "error: Cursor co-author trailer is not allowed in commits." >&2
  exit 1
fi
exit 0
